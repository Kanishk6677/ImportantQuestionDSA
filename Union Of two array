int doUnion(int arr[],int n,int brr[],int m)
{
unordered_map<int,int>map;
for(int i=0;i<n;i++)
{
map[a[i]++];
}
for(int i=0;i<m;i++)
{
map[b[i]++];
}
cout<<map.size();
// We have to print the union legth
